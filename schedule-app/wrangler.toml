# Cloudflare Pages Functions 配置
# 定时任务：每天北京时间 8:00 自动检查并推送到期提醒

name = "imauser-functions"
main = "functions/api/_worker.js"
compatibility_date = "2024-01-01"

# ──── Cron Triggers（自动定时执行） ────
# 格式: 分 时 日 月 周 (UTC时间)
# 北京时间 8:00 = UTC 0:00
[triggers]
crons = ["0 0 * * *"]
